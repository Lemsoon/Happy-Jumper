using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class createBackground : MonoBehaviour
{
    private GameObject _stars;
    // Start is called before the first frame update
    void Start()
    {
        _stars = this.gameObject;
        //instantiate stars for a number of blocks upwards
        //Randomize rotation of the stars to get variation
        //Instantiate stars for number of blocks to the left
        
    }

}